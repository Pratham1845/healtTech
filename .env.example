# HealthTech — Root Environment Example
# This file is safe to commit. Never commit the actual .env

# ──────────────────────────────────────────────
# These are set per-service, not at the root.
# Copy Backend/.env.example  →  Backend/.env
# ──────────────────────────────────────────────
